#define sqr(a) ((a)*(a))
#define sign(x) ((x)<0 ? -1:1)
#define inout(a,b) in_stream >> a; out_stream << "! " << b << " = " << a << "\n"
#define inout2(a,b) in_stream >> a; out_stream1 << "! " << b << " = " << a << "\n"; out_stream2 << "! " << b << " = " << a << "\n";

const double pi = M_PI;
const double pi_half = M_PI / 2.;
const double pi_double = M_PI * 2.;
